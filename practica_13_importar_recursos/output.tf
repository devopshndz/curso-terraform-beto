# output "ec2_public_ip" {
#     description = "Ip publica de la instancia"
#     value = aws_instance.public_instance.public_ip # instancia de aws, nombre de la instancia, la ip publica que no sabemos
# }
# # al agregar outputs, al finalizar el apply, estos aparecen en la terminal dandonos el valor que queremos conocer